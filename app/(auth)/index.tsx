import { View, Text } from 'react-native';
export default function AuthIndex() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>/auth is working ✅</Text>
    </View>
  );
}
